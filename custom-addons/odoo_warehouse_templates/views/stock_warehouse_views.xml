<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_warehouse_document_templates" model="ir.ui.view">
        <field name="name">stock.warehouse.document.templates</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet/group[1]" position="after">
                <notebook>
                    <page string="Document layout" name="warehouse_document_layout">
                        <group class="o_document_layout">
                            <group class="o_document_layout_company">
                                <field name="external_report_layout_id" invisible="1"/>
                                <field name="report_layout_id"
                                       string="Layout"
                                       widget="selection_badge"
                                       options="{'horizontal': true, 'size': 'sm'}"/>
                                <field name="layout_background" string="Background" widget="selection"/>
                                <field name="layout_background_image"
                                       options="{'accepted_file_extensions': 'image/*'}"
                                       invisible="layout_background != 'Custom'"
                                       required="layout_background == 'Custom'">
                                    Upload your file
                                </field>
                                <field name="font" string="Text" widget="selection"/>
                                <field name="logo" string="Logo" widget="image" options="{'size': [0, 50]}"/>
                                <field name="report_logo_max_height"/>
                                <label for="primary_color" string="Colors"/>
                                <div class="o_document_layout_colors d-flex align-items-end mb-2">
                                    <field name="primary_color" widget="color" class="w-auto m-0 me-1"/>
                                    <field name="secondary_color" widget="color" class="w-auto m-0"/>
                                </div>
                                <field name="report_address" options="{'resizable': true}"/>
                                <field name="company_details" options="{'resizable': false}"/>
                                <field name="report_header" string="Tagline" placeholder="e.g. Global Business Solutions" options="{'resizable': false}"/>
                                <field name="report_footer"
                                       string="Footer"
                                       placeholder="Write your phone, email, bank account, tax ID, ..."
                                       options="{'resizable': false}"/>
                                <field name="paperformat_id" widget="selection"/>
                                <field name="document_vat" string="Tax ID"/>
                                <field name="document_account_number"
                                       string="Bank Account Number"
                                       required="document_qr_code"
                                       placeholder="BE71096123456789"/>
                                <field name="document_qr_code" string="QR Code"/>
                            </group>
                        </group>
                    </page>
                    <page string="Emails &amp; PDF reports" name="warehouse_document_templates">
                        <group string="Sales emails">
                            <field name="sale_mail_template_quotation_id"/>
                            <field name="sale_mail_template_confirmation_id"/>
                        </group>
                        <group string="Accounting emails">
                            <field name="account_move_mail_template_invoice_id"/>
                            <field name="account_move_mail_template_credit_note_id"/>
                        </group>
                        <group string="Operations emails">
                            <field name="stock_picking_mail_template_id"/>
                        </group>
                        <group string="PDF reports (optional)">
                            <field name="invoice_report_id"/>
                            <field name="delivery_report_id"/>
                        </group>
                    </page>
                </notebook>
            </xpath>
        </field>
    </record>
</odoo>
